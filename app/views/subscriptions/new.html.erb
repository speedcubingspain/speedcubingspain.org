<% provide(:title, "Pago de la cuota de socio anual") %>
<% provide(:active_section, "association") %>
<%= render "layouts/page_wrapper", title: "Pago de la cuota de socio anual" do %>
  <%= form_tag subscriptions_path do %>
    <% if flash[:error].present? %>
      <div id="error_explanation">
        <p><%= flash[:error] %></p>
      </div>
    <% end %>
    <div class="amount">
      <p>El precio de la cuota de socio anual es de 12 €, que puedes pagar haciendo clic en el botón a continuación:</p>
    </div>
    <center>
      <script src="https://checkout.stripe.com/checkout.js" class="stripe-button"
        data-key="<%= Rails.configuration.stripe[:publishable_key] %>"
        data-label="Pagar con tarjeta"
        data-image="/stripe-aes_logo.png"
        data-name="AES"
        data-description="Cuota de socio anual"
        data-email="<%= @current_user.email %>"
        data-amount="1200"
        data-currency="eur"
        data-locale="es"
        data-zip-code="true">
      </script>
    </center>
  <% end %>
<% end %>
